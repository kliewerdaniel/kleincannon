fastapi>=0.110
uvicorn>=0.27
faster-whisper>=1.0.0
Pillow>=10.0
numpy>=1.24

# TTS engine: Qwen3-TTS (In-Context-Learning voice cloning) on Apple Silicon via
# MLX. `mlx-audio` pulls in mlx / mlx-lm / transformers and downloads the
# 4-bit model `mlx-community/Qwen3-TTS-12Hz-1.7B-Base-4bit` (~1.5 GB) on first use.
# Reference-clip transcription uses faster-whisper (already a dependency above).
mlx-audio>=0.4.2
