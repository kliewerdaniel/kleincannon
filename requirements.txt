fastapi>=0.110
uvicorn>=0.27
faster-whisper>=1.0.0
Pillow>=10.0
numpy>=1.24

# TTS fallback engine (Apache-2.0). The primary F5-TTS engine is the MLX Swift
# bundle `aufklarer/F5TTS-v1-Base-MLX-fp16`, driven by the `speech` CLI from
# speech-swift (a Swift package, not pip-installable). If `speech` is not on
# PATH, tts.py falls back to this pure-Python package. See scripts/install_deps.sh.
f5-tts>=1.0.0
